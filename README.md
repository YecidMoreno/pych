# PYCH

## How to install


```bash
#No funciona siempre
sudo apt-get install can-utils

cd scripts/can
sudo ./install.sh

git init
git submodules init

souurce activate.sh

python3 gui/main.py
  > build
```

